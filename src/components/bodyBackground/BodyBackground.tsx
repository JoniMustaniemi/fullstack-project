import { ReactElement } from "react";
import "./bodybackground.scss";

const BodyBackground = (): ReactElement => {
  return (
    <>
      <div className="backgroundImage" />
    </>
  );
};

export default BodyBackground;
